
When /^(?:the system|it)\s+needs to notify customer server about an incident$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^(?:the system|it)\s+will direct the subscriber to the existing contact link$/ do
  pending # express the regexp above with the code you wish you had
end

And /^(?:the system|it)\s+notifies\s+(customer service|the technical group)\s+to\s+(.*)$/ do |target_organization,
    action, table |
  # table is an optional Cucumber::Ast::Table
  pending # express the regexp above with the code you wish you had
end

